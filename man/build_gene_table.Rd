% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_tables.R
\name{build_gene_table}
\alias{build_gene_table}
\title{Join gene tables with optional annotations}
\usage{
build_gene_table(
  genes_df,
  gene_id_col = "gene_id",
  gene_symbol_col = "gene_symbol",
  orthology_df = NULL,
  expression_df = NULL,
  eqtl_df = NULL,
  variants_df = NULL,
  phenotypes_df = NULL,
  join_type = "left"
)
}
\arguments{
\item{genes_df}{Data frame with gene identifiers (required)}

\item{gene_id_col}{Name of gene ID column in genes_df (default: "gene_id")}

\item{gene_symbol_col}{Name of gene symbol column (default: "gene_symbol")}

\item{orthology_df}{Optional orthology mapping table}

\item{expression_df}{Optional expression data table}

\item{eqtl_df}{Optional eQTL results table}

\item{variants_df}{Optional variant annotation table}

\item{phenotypes_df}{Optional phenotype associations table}

\item{join_type}{Type of join to use ("left", "inner", "full")}
}
\value{
A data.table with joined gene information
}
\description{
Core function for joining gene tables with various annotation sources.
Follows relational database principles to combine gene-level data from
multiple sources into a unified table.
}
\examples{
# Basic usage with just genes
genes <- data.frame(gene_id = c("ENSMUSG001", "ENSMUSG002"),
                    gene_symbol = c("Abcb10", "Acsl5"))
result <- build_gene_table(genes)

# Add expression data
expr <- data.frame(gene_id = c("ENSMUSG001", "ENSMUSG002"),
                   CPM_heart = c(150.2, 89.5))
result <- build_gene_table(genes, expression_df = expr)

}
