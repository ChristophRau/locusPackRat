% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotype.R
\name{extract_phenotypes}
\alias{extract_phenotypes}
\title{Extract phenotypes by keyword matching}
\usage{
extract_phenotypes(
  phenotype_df,
  keywords = NULL,
  keyword_sets = NULL,
  use_sets = NULL,
  text_columns = NULL,
  case_sensitive = FALSE,
  match_type = "any"
)
}
\arguments{
\item{phenotype_df}{Data frame with phenotype descriptions}

\item{keywords}{Character vector of keywords to search for}

\item{keyword_sets}{Named list of predefined keyword sets (e.g., "cardiac", "metabolic")}

\item{use_sets}{Character vector of keyword set names to use}

\item{text_columns}{Columns to search for keywords (default: auto-detect)}

\item{case_sensitive}{Logical, should matching be case sensitive (default: FALSE)}

\item{match_type}{How to match keywords: "any", "all", "exact" (default: "any")}
}
\value{
Data frame with matched phenotypes and match details
}
\description{
Flexible phenotype extraction from various databases (MGI, IMPC, Open Targets)
using keyword matching. Supports custom keyword sets for different research areas.
}
\examples{
# Using custom keywords
pheno_df <- data.frame(
  gene = c("Abcb10", "Acsl5"),
  phenotype = c("cardiac hypertrophy", "insulin resistance")
)
cardiac_phenos <- extract_phenotypes(pheno_df, keywords = c("cardiac", "heart"))

# Using predefined keyword sets
results <- extract_phenotypes(pheno_df, use_sets = c("cardiac", "metabolic"))

}
