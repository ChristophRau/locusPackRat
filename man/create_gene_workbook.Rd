% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{create_gene_workbook}
\alias{create_gene_workbook}
\title{Create multi-sheet Excel workbook for gene curation}
\usage{
create_gene_workbook(
  gene_data,
  output_file,
  sheet_config = NULL,
  styles = NULL,
  freeze_panes = TRUE,
  conditional_format = TRUE,
  hyperlinks = TRUE
)
}
\arguments{
\item{gene_data}{Primary gene data table}

\item{output_file}{Path for output Excel file}

\item{sheet_config}{List defining which sheets to create}

\item{styles}{List of style specifications for formatting}

\item{freeze_panes}{Freeze first row and/or column (default: TRUE)}

\item{conditional_format}{Apply conditional formatting rules (default: TRUE)}

\item{hyperlinks}{Add hyperlinks to external resources (default: TRUE)}
}
\value{
Invisible NULL (creates file as side effect)
}
\description{
Generate comprehensive Excel workbooks for manual gene curation.
Each workbook contains multiple sheets with different data views.
}
\examples{
# Basic workbook creation
genes <- data.frame(
  gene = c("Abcb10", "Acsl5"),
  biotype = c("protein_coding", "protein_coding"),
  expression = c(150, 90)
)
create_gene_workbook(genes, "output.xlsx")

# Custom sheet configuration
config <- list(
  overview = TRUE,
  detailed = TRUE,
  variants = TRUE,
  phenotypes = TRUE,
  custom_sheets = list(
    high_priority = function(df) df[df$expression > 100, ]
  )
)
create_gene_workbook(genes, "output.xlsx", sheet_config = config)

}
